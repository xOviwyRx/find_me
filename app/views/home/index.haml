.container
  .row.mt-4
    .col
      .card.mb-3
        .card-body
          %h2.card-title Brand
          = render "brands/form"
  .row
    .col
      .card.mb-3
        .card-body
          %h2.card-title Users
          %p
            Total:
            %span#users_count= @brand.users_count
          #users
            = render @brand.users
          #user_form
            = render "users/form", user: User.new
  .row
    .col
      .card
        .card-body
          .card-title
            %h1 Task

            %p
              Try to find bug in brand model.
            %p
              Add new column to brand:
              %b users_count
            %p
              Brand require two new models.
            %p
              First model is setting. Brand has many settings. Model should have this fields:
              %b key, value
            %p
              Second model is users. Brand has multiple users. Users have multiple brands. Model should have this fields:
              %b first_name, last_name, email
            %p
              UPD: each user should have one setting record for each brand.
            %p
              brand name, user first_name, user last_name, user email should be saved without spaces, in down case. If any field include "test" this part should be cutted.
            %p
              On root page add input with brand name. Allow change brand name with autosave.
            %p
              Add ability to create/destroy new users for brand. Update users_count field.
            %p
              %b You must use only existing dependencies.
