= form_with model: @brand, url: brand_path, method: :patch, data: { controller: "autosave" } do |f|
  .mb-3
    = f.label :name, "Brand name", class: "form-label"
    = f.text_field :name, class: "form-control", data: { action: "input->autosave#save" }
    #brand_status.form-text
