.d-flex.align-items-center.justify-content-between.mb-2{id: dom_id(user)}
  %span= user_display_name(user)
  = button_to "Remove", user_path(user), method: :delete, class: "btn btn-danger action-btn", form: { data: { turbo_confirm: "Remove this user?" } }
